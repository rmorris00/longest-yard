#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ng2/bin/ng2-scaffolder" "$@"
  ret=$?
else 
  node  "$basedir/../ng2/bin/ng2-scaffolder" "$@"
  ret=$?
fi
exit $ret
